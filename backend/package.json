{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "seed": "node seed.js",
    "build": "esbuild index.js --bundle --platform=node --outfile=dist/index.js --external:bcrypt",
    "move-frontend": "rm -Rf dist && mv ./frontend/dist .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
